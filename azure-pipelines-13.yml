trigger: none
pr: none

resources:
  pipelines:
  - pipeline: build
    source: ci-build
    trigger: true

pool:
  vmImage: ubuntu-latest

jobs:
- job: DeployJob
  variables:
    imageTagFromBuild: $[ resources.pipeline.build.outputs['BuildStage.BuildJob.setVersion.imageTag'] ]
                                                             # 👆 stage name is now required in the path
  steps:
  - script: |
      echo "Received image tag: $(imageTagFromBuild)"