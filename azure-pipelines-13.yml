trigger: none

pr: none

resources:
  pipelines:
  - pipeline: build
    source: ci-build
    trigger: true

pool:
  vmImage: ubuntu-latest

variables:
  imageTagFromBuild: $[ resources.pipeline.build.outputs['BuildJob.setVersion.imageTag'] ]

steps:
- script: |
    echo "Received image tag: $(imageTagFromBuild)"